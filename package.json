{
  "name": "fiddlebops-refactored",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "extract-games": "node scripts/extract-games.js",
    "generate-pages": "node scripts/generate-pages.js",
    "rebuild-all": "npm run extract-games && npm run generate-pages",
    "backup-pages": "cp -r src/pages src/pages-backup-$(date +%Y%m%d%H%M%S)"
  },
  "dependencies": {
    "astro": "^5.11.0",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@stagewise/toolbar": "^0.5.0",
    "@types/node": "^24.0.10"
  }
}
