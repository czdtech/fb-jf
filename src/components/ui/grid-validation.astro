---
// Validation component to test grid classes
---

<div class="p-8 space-y-8">
  <h2 class="text-2xl font-bold text-center">Grid Class Validation</h2>
  
  <!-- Test Standard Grid Classes -->
  <div class="space-y-4">
    <h3 class="text-lg font-semibold">Standard Grid: grid gap-6 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5</h3>
    <div class="grid gap-6 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5">
      {Array.from({ length: 10 }, (_, i) => (
        <div class="bg-primary/20 p-4 rounded text-center">
          Item {i + 1}
        </div>
      ))}
    </div>
  </div>
  
  <!-- Test Featured Grid Classes -->
  <div class="space-y-4">
    <h3 class="text-lg font-semibold">Featured Grid: grid gap-8 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3</h3>
    <div class="grid gap-8 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3">
      {Array.from({ length: 6 }, (_, i) => (
        <div class="bg-secondary/20 p-4 rounded text-center">
          Featured {i + 1}
        </div>
      ))}
    </div>
  </div>
  
  <!-- Test Compact Grid Classes -->
  <div class="space-y-4">
    <h3 class="text-lg font-semibold">Compact Grid: grid gap-4 grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6</h3>
    <div class="grid gap-4 grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6">
      {Array.from({ length: 12 }, (_, i) => (
        <div class="bg-accent/20 p-2 rounded text-center text-sm">
          {i + 1}
        </div>
      ))}
    </div>
  </div>
  
  <!-- Container Classes Test -->
  <div class="space-y-4">
    <h3 class="text-lg font-semibold">Container Classes: w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8</h3>
    <div class="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 bg-muted/30 p-4 rounded">
      <p class="text-center">This container should be responsive with proper padding</p>
    </div>
  </div>
</div>