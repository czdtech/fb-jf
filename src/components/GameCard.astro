---
import type { Game } from '@/types'

export interface Props {
  game: Game
  variant?: 'grid' | 'list' | 'featured' | 'sidebar' | 'trending' | 'compact'
}

const { game, variant = 'grid' } = Astro.props

// 安全的URL处理
const gameUrl = game.slug
  ? game.slug.startsWith('http')
    ? game.slug
    : `/${game.slug}/`
  : '#'
---

{
  variant === 'sidebar' && (
    <a href={gameUrl} class="game-card game-card-sidebar">
      <div class="game-card-image-wrapper">
        <img src={game.image} alt={game.title} />
        <div class="game-card-overlay">
          <div class="game-card-play-button">
            <i class="fas fa-play" />
          </div>
        </div>
        <div class="game-card-title-popup">
          <span class="game-card-title-text">{game.title}</span>
        </div>
      </div>
    </a>
  )
}

{
  variant === 'grid' && (
    <div class="game-card game-card-grid">
      <div class="game-card-image">
        <img src={game.image} alt={game.title} />
        <div class="game-card-overlay">
          <button class="game-card-play-button">
            <i class="fas fa-play" />
          </button>
        </div>
      </div>
      <div class="game-card-content">
        <h3 class="game-card-title">{game.title}</h3>
        <p class="game-card-description">
          {game.description || '体验这款精彩的音乐创作游戏'}
        </p>
      </div>
    </div>
  )
}

{
  variant === 'featured' && (
    <div class="game-card game-card-featured">
      <div class="game-card-image">
        <img src={game.image} alt={game.title} />
        <div class="game-card-overlay">
          <button class="game-card-play-button">
            <i class="fas fa-play" />
          </button>
        </div>
      </div>
      <div class="game-card-content">
        <h3 class="game-card-title">{game.title}</h3>
        <p class="game-card-description">
          {game.description || '体验这款精彩的音乐创作游戏'}
        </p>
      </div>
    </div>
  )
}

{
  variant === 'trending' && (
    <a href={gameUrl} class="game-card game-card-trending">
      <div class="game-card-image-container">
        <img src={game.image} alt={game.title} />
        <div class="game-card-trending-overlay">
          <div class="game-card-trending-play">
            <i class="fas fa-play" />
          </div>
          <div class="game-card-trending-gradient" />
          <div class="game-card-trending-info">
            <h3 class="game-card-trending-title">{game.title}</h3>
          </div>
        </div>
      </div>
    </a>
  )
}

{
  variant === 'compact' && (
    <a href={gameUrl} class="game-card game-card-compact">
      <div class="game-card-compact-image">
        <img src={game.image} alt={game.title} />
        <div class="game-card-compact-overlay">
          <div class="game-card-compact-play">
            <i class="fas fa-play" />
          </div>
        </div>
        <div class="game-card-compact-gradient" />
        <div class="game-card-compact-info">
          <h3 class="game-card-compact-title">{game.title}</h3>
        </div>
      </div>
    </a>
  )
}
