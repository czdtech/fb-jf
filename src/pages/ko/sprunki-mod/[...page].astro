---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { SPRUNKI_MOD_HREFLANG } from '../../../data/hreflang';
import { toGameCardData } from '../../../lib/canonical-games';
export { getStaticPaths } from '../../sprunki-mod/[...page].astro';

const { page } = Astro.props;
const locale = 'ko';
const title = 'Sprunki Mod - Sprunki 게임 월드';
const description =
  'Sprunki를 기반으로 한 다양한 Mod, 스핀오프, 콜라보 작품들을 모아둔 페이지입니다. 브라우저에서 바로 무료로 플레이해 보세요.';
const canonicalUrl = 'https://www.playfiddlebops.com/ko/sprunki-mod/';
const ogImage = 'https://www.playfiddlebops.com/tw.jpg';
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Game',
  name: title,
  alternateName: 'playfiddlebops.com',
  url: canonicalUrl,
  description
};
---
<BaseLayout
  title={title}
  description={description}
  canonicalUrl={canonicalUrl}
  ogImage={ogImage}
  ogType="website"
  twitterCard="summary_large_image"
  jsonLd={jsonLd}
  hreflangLinks={SPRUNKI_MOD_HREFLANG}
  lang="ko"
>
  <section class="games">
    <div class="container">
      <h1>Sprunki Mod</h1>
      <p>
        Sprunki 관련 Mod와 스핀오프 게임들을 한자리에 모았습니다.
      </p>

      <div class="game-grid">
        {page.data.map((entry) => {
          const card = toGameCardData(entry, locale);
          return (
            <article class="game">
              <a href={card.href} title={card.title}>
                <div class="game-logo game-l-2">
                  <img src={card.thumbnail} alt={card.title} loading="lazy" width="300" height="300" />
                  <div class="game-overlay">
                    <p>{card.title}</p>
                  </div>
                </div>
              </a>
            </article>
          );
        })}
      </div>

      <div class="pagination">
        {page.url.prev && <a href={page.url.prev}>«</a>}
        <b class="current">
          {page.currentPage} / {page.lastPage}
        </b>
        {page.url.next && <a href={page.url.next}>»</a>}
      </div>
    </div>
  </section>
</BaseLayout>
