---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { INCREDIBOX_MOD_HREFLANG } from '../../../data/hreflang';
export { getStaticPaths } from '../../incredibox-mod/[...page].astro';

const { page } = Astro.props;
const prefix = '/zh';
const title = 'Incredibox Mod - Incredibox 游戏世界';
const description =
  '浏览各类 Incredibox Mod、版本与同人混音作品，在浏览器中直接免费体验，无需下载。';
const canonicalUrl = 'https://www.playfiddlebops.com/zh/incredibox-mod/';
const ogImage = 'https://www.playfiddlebops.com/tw.jpg';
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Game',
  name: title,
  alternateName: 'playfiddlebops.com',
  url: canonicalUrl,
  description
};
---
<BaseLayout
  title={title}
  description={description}
  canonicalUrl={canonicalUrl}
  ogImage={ogImage}
  ogType="website"
  twitterCard="summary_large_image"
  jsonLd={jsonLd}
  hreflangLinks={INCREDIBOX_MOD_HREFLANG}
  lang="zh-CN"
>
  <section class="games">
    <div class="container">
      <h1>Incredibox Mod</h1>
      <p>
        精选 Incredibox 系列 Mod 与衍生版本，适合喜欢混音与创作的玩家。
      </p>

      <div class="game-grid">
        {page.data.map(({ astronaut }) => {
          const [href, thumbnail, gameTitle] = astronaut;
          const localizedHref = href.startsWith('http') ? href : `${prefix}${href}`;

          return (
            <article class="game">
              <a href={localizedHref} title={gameTitle}>
                <div class="game-logo game-l-2">
                  <img src={thumbnail} alt={gameTitle} loading="lazy" width="300" height="300" />
                  <div class="game-overlay">
                    <p>{gameTitle}</p>
                  </div>
                </div>
              </a>
            </article>
          );
        })}
      </div>

      <div class="pagination">
        {page.url.prev && <a href={page.url.prev}>«</a>}
        <b class="current">
          {page.currentPage} / {page.lastPage}
        </b>
        {page.url.next && <a href={page.url.next}>»</a>}
      </div>
    </div>
  </section>
</BaseLayout>
